#pragma once
#include "pch.h"

namespace ge
{
    // sync file i/o
    // asyn file i/o: epoll,

    // for shader
    // non-binary and binary

    // external linking
    extern std::string loadFile(std::string const &fpath);
}